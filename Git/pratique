// cloner un rep , le modifier et le pusher 
Dans github, cliquer sur "+" + import reposotory + coller le lien du repo à cloner + choisir nom.....
Après, dans repo local : git clone ......


// Méthode pour créer un répo git/github
- créer un new répo sur github : git remote add origin https://github.com/amlas2017/ansible-jenkins-install.git
- en local : 
   * dans le dossier à mettre dans le répo: 
        git init
        git add .
        git commit -m "1er commit"
        git config --global user.name "amlas2017"
        git config --global user.email amlas2010@live.fr
        git remote add origin https://github.com/amlas2017/ansible-jenkins-install.git
        git push -u origin master

// créer une branche dev : git branche dev
// git branch  (voir on est sur quelle branche)
// git checkout dev (se positionner dans dev)
// git checkout master
// git merge dev  ( fallait etre dans master pour faire un merge)



